name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov

      - name: Run tests
        run: |
          pytest src/ --tb=short -v --cov=src --cov-report=term-missing || true

      - name: Smoke test CLI (list)
        run: python src/hardware_specs.py list

      - name: Smoke test CLI (report)
        run: python src/hardware_specs.py report

      - name: Smoke test CLI (export)
        run: python src/hardware_specs.py export
